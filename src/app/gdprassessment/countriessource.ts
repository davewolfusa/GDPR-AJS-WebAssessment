export class CountriesSource {
  
    html: string;
    title: string;
    group: string;
  
    constructor(html: string, title: string, group: string) {
      this.html = html;
      this.title = title;
      this.group = group;
    }
}
